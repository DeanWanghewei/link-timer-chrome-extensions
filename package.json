{
  "name": "link-timer",
  "version": "1.5.0",
  "description": "一个可以定时提醒访问链接的Chrome扩展",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "chrome-extension",
    "scheduler",
    "links",
    "timer"
  ],
  "author": "Link Timer开发者",
  "license": "MIT",
  "devDependencies": {
    "archiver": "^5.3.2"
  }
}
